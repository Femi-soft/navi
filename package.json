{
  "name": "navi",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "NAVI financial decision and policy-constrained execution system",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @navi/web",
    "build": "npm run build --workspace @navi/web",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test tests/safety.test.ts",
    "validate": "npm run typecheck && npm run test && npm run build"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "decimal.js": "^10.6.0",
    "next": "^16.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "caniuse-lite": "file:../../../AppData/Local/Temp/caniuse-lite-1.0.30001809.tgz",
    "typescript": "^5.9.0"
  }
}
