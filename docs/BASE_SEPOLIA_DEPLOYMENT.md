# Base Sepolia protocol-validation deployment

Reviewed: 2026-08-22

Status: guarded testnet deployment complete and independently verified. The executor remains paused and the Aave adapter remains unapproved. Exact creation and runtime bytecode matches are published on Sourcify for every deployed contract. Independent adapter review remains required before any allowlisting or unpausing decision.

## Network isolation

- network: Base Sepolia
- chain ID: `84532`
- runtime RPC variable: `BASE_SEPOLIA_RPC_URL`
- deployer key variable: `BASE_SEPOLIA_DEPLOYER_PRIVATE_KEY`
- dedicated funded deployer: `0xa24289448904ce7a4126448735D6fA0C7f246b72`
- deployment module: `ignition/modules/NaviBaseSepolia.ts`
- policy manager V2: `0xE8e57B04986F26296A8f86582a9a3cdfdb6D4DF7`
- executor V2: `0x9D110a71F36a4fb1Feaa06E3F76C2e1136C9ACBa`
- Aave adapter V2: `0xA3E6fA5a29b36D83eDF05a4748AbF627E87b5509`

The Base Sepolia executor, policy manager, deployer, RPC, deployment artifacts and monitoring evidence are separate from X Layer testnet chain `1952`. No address or approval transfers between the two environments.

At `2026-08-19T09:16:53.482Z`, the replacement authenticated provider returned chain ID and block-number metadata but HTTP `503` with no result for balance, bytecode and contract-call methods. Base's official public RPC independently returned `503` for the same state reads. Base's documented PublicNode fallback passed the protocol invariants and was explicitly acknowledged for this testnet-only deployment. It is not approved as a production provider.

The three creation receipts succeeded at Base Sepolia blocks `45825029`, `45825036`, and `45825042`. Independent verification at `2026-08-22T17:06:48.713Z` confirmed deployed bytecode, the expected constructor bindings, dedicated-deployer ownership, paused executor state, and an empty adapter allowlist.

Sourcify v2 independently reported `exact_match` for creation and runtime bytecode for the policy manager, executor, and Aave adapter at `2026-08-22T17:11:19Z`, `2026-08-22T17:11:23Z`, and `2026-08-22T17:11:27Z`, respectively.

The operator reported successful manual BaseScan Standard JSON verification for all three contracts on 2026-08-22 using the exact Ignition compiler inputs and ABI-encoded constructor arguments generated by `npm run contracts:prepare:basescan`. BaseScan's Cloudflare challenge blocked independent rendered-page inspection, and its credentialed V2 API was unavailable, so the separate BaseScan badge remains operator-confirmed rather than machine-confirmed in this record.

## Aave V3 USDC candidate

Official source: Aave address book `AaveV3BaseSepolia.sol`, pinned at commit [`dd5a718d6739342882dd3327739dc037c4fd0028`](https://github.com/aave-dao/aave-address-book/commit/dd5a718d6739342882dd3327739dc037c4fd0028). Retrieved 2026-08-19.

- pool: `0x8bAB6d1b75f19e9eD9fCe8b9BD338844fF79aE27`
- USDC: `0xba50Cd2A20f6DA35D788639E581bca8d0B5d4D5f`
- aUSDC: `0x10F1A9D11CDf50041f3f8cB7191CBE2f31750ACC`
- USDC decimals: `6`
- maximum per action: `1000.000000` USDC
- permitted actions: supply USDC for the initiating user; withdraw an exact aUSDC amount back to that user
- prohibited actions: borrowing, collateral configuration, swapping, arbitrary targets/assets, native value, max-uint withdrawal and retained adapter custody

The deployment module creates `NaviPolicyManagerV2`, `NaviExecutorV2` and `AaveSupplyWithdrawAdapterV2`. It does not approve the adapter or unpause the executor. A fresh authenticated RPC probe must confirm chain ID, bytecode, reserve configuration, proxy implementations and current protocol state before broadcast.

### Live preflight evidence

At block `45681550` retrieved at `2026-08-19T09:23:17.575Z`, Base's documented PublicNode fallback confirmed all pinned Aave relationships and fresh state:

- pool provider runtime hash: `0xdabec01ff02f2b5983c5095b31963c19e108624289f9614e0aaf33bbbcbbf0f6`
- pool proxy runtime hash: `0xc97b7e16966b0eafd8bc9eebf8937ee34f1bf0f3d3907d978dedc6148811b0f5`
- pool implementation: `0xE9fb54369E85b9714F9d86366D918c5C7C87783A`
- pool implementation runtime hash: `0xeb2f47a1422f8d12a135c1d1bd97eddb9980bfcc50e08206ae9b3a8c81c87db7`
- USDC runtime hash: `0x76d471b1a3bee182d4a7271b3e2fd20ac466ac7286ed9f9492cca0b5d6a83da0`
- aUSDC proxy runtime hash: `0x3b36b7f4d52e12a724fa5a94521b1810ad9cbcdcd5d00e98bfe93dbb5b7bfae8`
- aUSDC implementation: `0xF83D6A049FE62b8A1270525E1516F93b61C7D113`
- aUSDC implementation runtime hash: `0xdada526caf0a41d4d496294962fefc4720795a9ace2fce2c14d73b165d0632e9`
- reserve, aToken asset and aToken pool relationships matched the pinned registry; USDC reported symbol `USDC` and six decimals

## Centrifuge RWA candidate

Official source: Centrifuge `env/base-sepolia.json`, pinned at commit [`304257ce844356b5302f44135dbea9ee2447ab2f`](https://github.com/centrifuge/protocol/commit/304257ce844356b5302f44135dbea9ee2447ab2f). Retrieved 2026-08-19.

The registry publishes the Base Sepolia protocol core, including root `0x1Fa8a2fe2eAE2373B03Fa313E016605EB86cBae8` and vault registry `0x9E7d9003c07a896d5E7b075fA6F4D1d7a2fDf672`, with deployment transactions. It does not identify a currently investable test vault, its asset, eligibility rules or an exit path.

`BoundedERC4626AdapterV2` is implemented and locally tested as an immutable single-vault boundary. It must not be deployed until an official Centrifuge vault and asset pass bytecode, source, audit, eligibility, liquidity, deposit/redeem and freshness review. A testnet integration demonstrates protocol mechanics only; it does not establish legal ownership of or a claim on real-world assets.

## Guarded sequence

1. Use the explicitly approved Base-documented PublicNode fallback only for this Base Sepolia deployment; the deployment script requires `-AcknowledgePublicFallback`.
2. Fund only the dedicated deployer with Base Sepolia ETH.
3. Refresh and record every Aave target, implementation, reserve and asset invariant immediately before broadcast.
4. Run full validation, Slither and an independent adapter review.
5. Deploy V2 contracts and the Aave adapter paused and unapproved.
6. Verify receipts, bytecode, constructor bindings, owner, pause state and empty allowlist independently.
7. Approve a capped canary only after a separate explicit release decision and provider-backed simulation.
